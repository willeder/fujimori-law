.venv-xlsx/
.venv/

# Node / Next.js
node_modules/
.next/
next-env.d.ts
out/

# pnpm ローカルストア（リポジトリには含めない。382MB/1万件超）
.pnpm-store/

# 環境変数
.env
.env.local
.env*.local

# Prisma 生成物
/src/generated/

# 生成データ（再生成可: npm run data:build）。大容量のためコミットしない
/public/data/*.json

# 実顧客データ・PII（接触履歴/入金/和解/実案件ファイル等）。GitHubに上げない
/docs/data/

# 一時QA/検証スクリプト（実パスワードを含む場合があるためコミット禁止）
/scripts/_qa*.mjs

# Vite ビルド成果物
dist/
dist-ssr/
*.local

# TypeScript インクリメンタルビルドキャッシュ（再生成可）
*.tsbuildinfo

# Vercel CLI
.vercel

# Claude Code ローカル設定（端末固有・共有しない）
.claude/settings.local.json

# Claude(Cowork)作業用の一時ファイル（コミット禁止）
# ステージング用アーカイブと、マウント経由で削除できないファイルの退避先
__claude_stage*.tar.gz
/_to_delete/

# kintone から取得した生データ（個人情報・添付ファイルを含むためコミットしない）
docs/data/kintone/
